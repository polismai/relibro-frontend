export type FilterOptions = {
  category: string;
  department: string;
  onlyAvailable: boolean;
  subject: string;
  schoolYear: string;
  genre?: string;
  minPrice?: number;
  maxPrice?: number;
  sortBy?: string;
  school?: string;
  search?: string;
};